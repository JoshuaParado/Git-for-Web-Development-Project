https://codepen.io/JCoder609/pen/xxEVeJY
 1. What is Semantic HTML? It describes the purpose of the element and the content type that is inside that element. 
    2. What is HTML used for? The markup language that is designed to display the web browser.
    3. What is an attribute and where do we put it? An attribute defines the character of an element. It is placed inside the opening tag.
    4. What is the h1 tag used for? How many times should I use it on a page? H1 is used for the first heading, meaning the larget heading.You can use the <h1> tag as many times as you want but you shouldnt exceed 6 <h> tags.
    5. Name two tags that have required attributes <p> <a>
    6. What do we put in the head of our HTML document? <head>
    7. What is an id? An id is to identify an html element.
    8. What elements can I add an id to? <h1> <p>
    9. How many times can I use the same id on a page? Once 
    10. What is a class? You must make a class in html in order to style it in css
    11. What elements can I add a class to? <h1> <div> <p> <section> 
    12. How many times can I use the same class on a page? As many times you want
    13. How do I get my link to open in a new tab?<a href""
    14. What is the alt attribute in the image tag used for? Alt attribute provides alternative information to the img.
    15. How do I reference an id? Once you have your id made in your html doc, you must reference that in your css by typing in # before the id
    16. What is the difference between a section and a div? The div tag is an empty container grouping block elements to style them in css. The section tag creates independent sections in your web page.
    17. What is CSS used for? Css is used to style your html elements.
    18. How to we select an element?Example - every h2 on the page h2 {

}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one The id can only be applied to one element while css can apply to multiple elements. For ex: <h1 id="myHeader">My Header</h1> and will be used to style that id in css like so #myheader {} I can apply multiple elements in css with my <p> tag like this .p{}
    20. How do we select classes in CSS? You must create a class in html then in front of that class name, you put a . {}
    21. How do we select a p element with a single class of “human””? p.human{} 
    22. What is a parent child selector? When would this be useful? A parent child selector selects all elements that are a direct child of that element. This would be useful when you want to style an element where the parent is present. 
    23. How do you select all links within a div with the class of sidebar? div.sidebar {}
    24. What is a pseudo selector? a keyword added to a selector that creates a special state of the chose element. For ex: :hover
    25. What do we use the change the spacing between lines? line-height:
    26. What do we use to change the spacing between letters? letter-spacing:
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform:
    28. How do I add a 1px border around my div that is dotted and black? Type border: div.class { 
1px dotted black;
}
    29. How do I select everything on the page? The * selector selects all elements 
    30. How do I write a comment in CSS? /* comment and end with */
    31. How do I find out what file I am in, when I am using the command line? pwd
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm, i should be careful because I need to make sure you dont delete relevant files 
    34. Why should I use version control? It tracks your changes over time
    35. How often should I commit to github? Everytime you make a change to your repository
    36. What is the command we would use to push our repo up to github? git push 
    37. Walk me through Lambda's git flow. $git clone $git pull

Stretch Questions

    1. What is the difference between an inline element and a block element? Inline elements doesnt force a new line to begin in the doc flow while block elements forces a line break to occur.
    2. What happens when an element is positioned absolutely? It no longer exists in the regular doc layout.
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? Margin, position: absolute.
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default. Div is display:block; Span is inline:block; Section 
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? Box model is used in css when styling design and layout of your html. It wraps around every html element which includes margins, borders, padding. To make elements respect the width weve them calculate an elements full size.
